  {{> aside_character}}
  {{> main_character}}
  {{> settings}}
  <script>
  const edit = (id) => {
        window.location.href = `character/information/${id}/edit`
    }

  const profile = (id) => {
        window.location.href = `character/information/${id}/profile`
    }
</script>